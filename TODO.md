# TODO: Merge index.js and server.js into App.js for Render Deployment

## Steps:
- [x] Create App.js with merged server setup, in-memory data, API routes (/api/posts), and view routes (/, /new, /edit/:id, CRUD actions).
- [x] Update package.json: Set "main" to "App.js" and "start" script to "node App.js".
- [x] Commit changes to Git: Add all, commit with message "Merge index.js and server.js into App.js for Render deployment", and push to origin.
- [ ] (Optional) Remove original index.js and server.js files after verification.
- [ ] Test locally: Run `npm start` and verify the app works on localhost:3000 (views load, CRUD operations function).
